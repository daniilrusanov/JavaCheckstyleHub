# Lombok project configuration
# Copy @Qualifier to generated constructor parameters so that @RequiredArgsConstructor
# works correctly with Spring's bean disambiguation (e.g. in AnalysisService).
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
